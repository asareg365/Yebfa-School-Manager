
{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "Information about users and their roles within the system.",
      "type": "object",
      "properties": {
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["super_admin", "owner", "staff", "student"] },
        "institutionId": { "type": "string", "description": "Reference to the school/institution this user belongs to." },
        "displayName": { "type": "string" }
      },
      "required": ["email", "role"]
    },
    "Institution": {
      "title": "Institution",
      "description": "Educational institution details.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "ownerEmail": { "type": "string" },
        "location": { "type": "string" },
        "type": { "type": "string" },
        "gradeLevel": { "type": "string", "description": "Category of grade levels (e.g. Basic, Secondary)." },
        "specificGrades": { "type": "string", "description": "Specific range of grades covered." },
        "subscriptionPlan": { "type": "string", "enum": ["basic", "premium", "enterprise"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["active", "pending_review", "suspended"] },
        "academicYear": { "type": "string" },
        "currentTerm": { "type": "string" }
      },
      "required": ["name", "ownerEmail"]
    },
    "Student": {
      "title": "Student",
      "description": "Comprehensive student enrollment record.",
      "type": "object",
      "properties": {
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "gender": { "type": "string" },
        "gradeLevel": { "type": "string" },
        "studentId": { "type": "string" },
        "status": { "type": "string", "enum": ["active", "suspended", "graduated"] },
        "institutionId": { "type": "string" },
        "dateOfBirth": { "type": "string", "format": "date" },
        "parentName": { "type": "string" },
        "parentPhone": { "type": "string" },
        "homeAddress": { "type": "string" }
      },
      "required": ["firstName", "lastName", "gradeLevel", "institutionId", "studentId"]
    },
    "Staff": {
      "title": "Staff Member",
      "description": "Institutional employee record.",
      "type": "object",
      "properties": {
        "fullName": { "type": "string" },
        "role": { "type": "string" },
        "department": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phoneNumber": { "type": "string" },
        "staffId": { "type": "string" },
        "joiningDate": { "type": "string", "format": "date" },
        "institutionId": { "type": "string" },
        "assignedClasses": { "type": "string", "description": "Comma-separated list of assigned classes." },
        "assignedSubjects": { "type": "string", "description": "Comma-separated list of assigned subjects." }
      },
      "required": ["fullName", "role", "institutionId", "staffId"]
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profiles. Super Admins: asareg365@gmail.com, frankyeb@gmail.com."
    },
    "/institutions/{institutionId}": {
      "schema": "Institution",
      "description": "Details for each school instance."
    },
    "/students/{studentId}": {
      "schema": "Student",
      "description": "Global student records partitioned by institutionId."
    },
    "/staff/{staffId}": {
      "schema": "Staff",
      "description": "Global staff records partitioned by institutionId."
    }
  }
}
